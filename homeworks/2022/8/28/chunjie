5.7.1
  现代的微处理器称为 superscaler，可以在每个时钟周期乱序执行多个操作。
  主要有指令控制单元和执行单元组成，乱序的处理器需要更大更复杂的硬件，也可以达到更高的指令并行度。
  ICU 从高速缓存读取指令，指令高速缓存可能会在指令执行前很早就进行取指并译码然后发送到高速缓存中。ICU 也有分支预测的技术。
  每个时钟周期可以读取多条操作，每个操作被分到相应的功能单元来实现。
