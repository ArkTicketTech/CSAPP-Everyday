5.5 减少过程调用
  过程调用会带来开销，而且妨碍大多数形式的程序优化。
  
5.6 消除不必要的内存引用
  循环中读取同一个地址空间时，可以通过引入临时变量，这个临时变量在循环中存在寄存器中，可以显著提高性能。
  
5.7 理解现代处理器
  到目前为止的优化是基于降低了过程开销和消除了一些妨碍优化的因素，并不涉及目标机器的特性。
  现代微处理了不起的一点在于，其中可能有很多指令级并行，同时又呈现出一种简单的顺序执行指令的现象。
  但当一系列操作必须按照严格顺序执行时，会遇到延迟界限。吞吐量界限描述了处理器的原始计算能力，是程序性能的终结界限。
