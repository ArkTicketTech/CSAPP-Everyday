4.5.10 未完成的工作
  PIPE 还缺少一些实际中必须的关键特性。
  1.多周期指令
  Y86-64 现在所有的指令都包含了一些简单的操作，例如数字加法。这些操作可以在一个周期内完成。一些完整指令集中的复杂操作，例如浮点数运算则需要多个周期。
  为了这些操作，我们需要额外的硬件。
  2.与存储系统的接口
  我们假设取指单元和数据内存可以在一个周期完成对任意位置的读写，但是从虚拟地址翻译成物理地址并取值在一个周期内是无法完成的。
  更糟糕的是值可能会在硬盘中，这可能会需要成千上万个周期来读写。
