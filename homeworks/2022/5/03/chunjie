4.1 Y86-64 指令集体系结构
  定义一个指令集体系结构包括定义各种状态单元，指令集和编码，一组编程规范和异常处理。

4.1.1 程序员可见状态
  Y86 程序中每条指令都会读取或修改处理器状态的某些部分。这成为程序员可见状态。
  类似 x86，除了 %rsp 作为栈指针其他寄存器没有固定的含义和固定值。
  内存从概念上来说就是一个很大的字节数组，虚拟内存系统向程序提供了一个单一的字节数组映像。
  程序状态的最后一个部分是状态码 Stat，表明了程序执行的总体状态。
  
4.1.3 指令编码
  每条指令需要 1~10 个字节不等，取决于需要的字段。
  第一个字节表示指令的类型，高 4 位是代码部分，低 4 位是功能部分。功能值只在一组相关指令公用一个代码时才有用。
  15 个寄存器都有一个相对应范围在 0 到 0xE(0xF %r15) 之间的寄存器标识符。程序寄存器存在 cpu 中的一个寄存器文件中，这个文件就是一个以寄存器 id 作为地址的随机访问存储器。
  
