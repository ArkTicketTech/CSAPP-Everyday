4.1.3 指令编码(continue)
  指令集一个重要性质是字节编码必须有唯一解释。每条指令第一个字节有唯一的代码和功能组合，保证了处理器可以无二义性执行目标代码。
  x86-64 其实是 CISC 指令集？
  早期 CISC 指令集由于微处理器发展的很快，根据新的功能需要加了很多指令。
  后来发现很多加到指令集中的高级指令很难被编译器产生，可以用更简单的指令集来产生高效的代码，也可以优化硬件，因此提出了 RISC 设计概念。
  
