4.3.4 SEQ 阶段的实现
  取址阶段
    以 PC 作为第一个字节的地址，读出 10 个字节。
  译码和写回阶段
    这两个阶段都要访问寄存器文件。
    根据指令代码以及寄存器指示值，可能还有条件信号，可能产生 4 个寄存器 ID。
  执行阶段
    这个单元根据 alufun 的信息执行 ADD/SUBSTRACT/AND/EXCLUSIVE-OR。
  访存阶段
    访存阶段控制块产生内存地址和输入数据和一个标识写还是读的控制信号。
  更新 PC 阶段
    最后一个阶段会产生 PC 的新值，依据指令类型和是否选用分支会有不同值产生。
