  4.3 Y86-64 的顺序实现
    顺序的处理器会处理一条完整指令所需的所有步骤，这样会需要一个很长的时钟周期。
    
  4.3.1 将处理组织成阶段
    通常处理一条指令包括很多操作，可以将不同的指令的操作序列总结为一个框架：
    取指：取指从内存读取指令
    译码：读取操作数
    执行：执行指令操作
    访存：把数据写入内存或从内存读数
    写回：将最多两个结果写回寄存器
    更新 PC：将 PC 设置为下一条指令
    
    处理器设计降低复杂度的方法是让不同指令共享尽可能多的硬件，即用同一套框架来描述所有的指令。
    不同指令可以不用按同一个顺序执行所有上述操作。
