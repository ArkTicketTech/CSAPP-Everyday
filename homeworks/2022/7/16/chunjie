4.4.3 流水线的局限性
  实际情况中会有其他一些因素降低一个理想化的流水线系统的效率。
  1. 不一致的划分。对硬件设计者来说，将系统计算设计划分成一组具有相同延迟的阶段是一个严峻的挑战，但流水线每个阶段需要预留的用时取决于耗时最长的阶段。
  2. 流水线过深，收益反而下降。流水线中的流水线寄存器需要的耗时会随着流水线的深度而上升。

4.4.4 带反馈的流水线系统
  相邻的指令很可能是相关的，例如需要读写上一条指令的结果或是需要指令控制流的结果来决定需要的指令。
  通过改变系统的行为来获得流水线是不可接受的。

4.5 Y86-64 的流水线实现
  我们在普通流水线上做一些修改就能实现一个高效的可以处理数据和控制相关的流水线。

4.5.1 SEQ+: 重新安排计算阶段
  我们把 PC 移到时钟周期开始时计算，对于流水线中活动的时序可以工作的更好。
