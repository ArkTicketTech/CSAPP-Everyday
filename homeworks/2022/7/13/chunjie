4.4 流水线的通用原理
  流水线话的系统中待执行任务被划分成了若干个独立的阶段。
  流水线化的一个重要特性就是提高了系统的吞吐量。

4.4.1 计算流水线
  流水线中每个阶段完成指令执行的一部分。
  每个时钟周期一条指令离开系统，一条指令进入系统。
  
4.4.2 流水线操作的详细说明
  在组合逻辑块之间采用时钟寄存器的简单机制足够控制流水线的指令流，时钟周期需要合理选择使信号有足够时间通过组合逻辑。
