4.5.2 插入流水线寄存器
  在各个阶段之间插入 5 种不同的流水线寄存器。

4.5.3 对信号进行重新排列和标号
  在流水线化的设计中与各个指令相关的值会有多个版本一起流动。需要使用正确版本的信号。
  
4.5.4 预测下一个 PC
  流水线化的设计就是每个时钟周期一条指令，意味着必须在取出当前指令后就决定下一条指令。但很多情况下，例如取出的是条件分支指令，要等到指令执行完毕之后才能真正获得下一条指令。
  选择通过预测 PC 的值来达到每个时钟周期一条指令的目的。
