jmp *%rax 使用寄存器%rax中的值作为跳转目标和指令
jmp *(%rax) 从内存中读取跳转目标，并使用%rax中的值作为读取地址
实现条件操作的传统方法是通过条件控制转移，即当条件成立时，程序遵循一条执行路径，当条件不成立时，程序遵循另一条执行路径。这种机制简单而通用，但在现代处理器上可能非常低效。另一种策略是通过数据的有条件传输。这种方法计算一个条件操作的两个结果，然后根据该条件是否成立来选择一个结果。