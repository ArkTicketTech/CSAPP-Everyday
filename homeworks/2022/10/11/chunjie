6.1.1 随机访问存储器(continued)
  6. 非易失性存储器
  如果断电了，DRAM 和 SRAM 会丢失信息，非易失性存储器即使是断电后仍然保持信息的存储器。
  EPROM 用石英窗口保存信息，可以用紫外线来改变信息，改变信息和重编程的次数可以到 1000 次。EEPROM 是印在电路上的重编程次数更多的非易失性存储器。
  闪存和 SSD 是基于 EEPROM 的。
  7. 访问主存
  数据通过总线在处理器和 DRAM 间来回。
  读事物是从主存传数据到 CPU，写事物是从 CPU 到主存。
