关于虚拟地址：

操作系统提供⼀种映射机制，将不同进程的连续虚拟地址和不同内存的物理地址映射起来。 进程要访问虚拟地址的时候，由操作系统转换成不同的物理地址，这样不同的进程运⾏的时候，写⼊的是不同的物理地址。这样做的目的：
保证不同进程分配的内存地址是不同的，但是进程的虚拟地址是连续的
程序运行时，PC 计数器根据时钟信号按顺序读取虚拟地址，一条一条执行下去
进程持有的虚拟地址会通过 CPU 芯⽚中的内存管理单元（MMU）的映射关系，来转换变成物理地址，然后再通过物理地址访问实际内存地址。