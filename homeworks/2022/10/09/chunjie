6.1.1 随机访问存储器(continued)
  3. 传统的 DRAM
  DRAM 芯片中的单元被分成 d 个超单元，每个超单元是 w 个单元组成。
  每个 DRAM 芯片都会连接到内存控制器，可以一次传至/传出 w 位。
  4. 内存模块
  DRAM 芯片封装在内存模块中插到主板的扩展槽。
  每个超单元存储主存的一个字节，相应地址为 (i, j) 的 8 个超单元表示 64 位字。
  5. 增强的 DRAM
  还有一些增强模式的 DRAM 例如快页模式 DRAM，扩展数据输出 DRAM，同步 DRAM，双倍数据速率同步 DRAM，视频 DRAM。
