指令流水作业：
把一个指令周期拆分成“取指令 - 指令译码 - 执行指令”这样三个部分，那这就是一个三级的流水线。
五级流水作业：
进一步把“执行指令”拆分成“ALU 计算（指令执行）- 内存访问 - 数据写回”，就变成一个五级的流水线。表示在同一个时钟周期里面，同时运行五条指令的不同阶段。这个时候，虽然执行一条指令的时钟周期数变成了 5，但是可以把 CPU 的主频提得更高了。不需要确保最复杂的那条指令在时钟周期里面执行完成，只要保障一个最复杂的流水线级的操作，在一个时钟周期内完成就好了。
注意：流水线技术并不能缩短单条指令的响应时间这个性能指标，但是可以增加在运行很多条指令时候的吞吐率。