4. 处理器体系结构
  处理器执行的指令被编码为由一个或多个字节序列组成的二进制格式。一个处理器支持的指令和指令编码称为指令集体系结构。
  处理器家族有 Intel IA32 和 x86-64, IBM/Freescale Power 和 ARM 等。相互之间不兼容，但同一处理器家族的不同厂商的处理器保持兼容。
  
  现代处理器实际工作方式不是简单的顺序指令执行，通过同时处理多条指令的不同部分处理器可以获得更高的性能。
  一个流水线化的处理器将每条指令分解成好几步，每个步骤由独立的硬件或者步骤来完成。处理器可以同时处理很多指令的不同阶段。
  为了保留顺序行为，处理器会进行“预测”指令，比如在判断逻辑执行时预测某一个分支会进入处理。这样也会带来冲突的处理。
