7.6 符号解析
  符号解析是将引用和可重定向目标文件符号表的一个确定符号练习起来。
  编译器只允许每个模块中的本地符号和静态变量有唯一定义。
  对于全局符号编译器会生成一个符号表表目，由链接器在所有输入模块中进行匹配。
  
7.6.1 链接器如何解析多处定义的全局符号
  函数和初始化了的全局变量只能有一处定义，也会在链接器选择的时候优先于未初始化的全局变量。
  这样的规则很容易会产生一些细小的错误并且不会被编译器报出来。
