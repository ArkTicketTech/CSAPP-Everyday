7.3 目标文件
  目标文件有三种形式：
    可重定位目标文件，可以在编译时和其他可重定位目标文件合并成一个可执行文件。由编译器生成。
    可执行目标文件，可以直接拷贝到存储器执行。由链接器生成。
    共享目标文件，可以在运行时动态加载。由编译器生成。
    
7.4 可重定位目标文件
  现代大多数操作系统使用 ELF 文件格式表示可重定位目标文件，ELF 文件以一个 ELF 头开始，描述了可执行文件的各种信息。
  
7.5 符号和符号表
  每个可重定位目标模块 m 都有一个符号表，包含了定义和使用的符号。
  有三种不同的符号：
    由 m 定义，并可以被其他模块使用的全局符号。
    由其他模块定义，并可以被 m 引用的全局符号。
    只被 m 定义和使用的本地符号。
    
