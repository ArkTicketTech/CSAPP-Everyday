内核对于所有的进程，不但物理内存只有一份，虚拟内存也是只有一份：
● A进程用户态访问x虚拟地址和B进程用户态访问x虚拟地址是不同的虚拟地址：
  ○ 即A进程用户态在x虚拟地址里面放了一个数值w，B进程用户态的x虚拟地址看不到w，对应是不同的物理地址。
  ○ 这样保证了进程在内存使用上的隔离性
● A进程内核态访问的y虚拟地址，和B进程内核态访问的y虚拟地址，是通一个虚拟地址，也对应相同的物理地址。
  ○ 即A进程内核态在y虚拟地址方一个数值n，B进程的内核态如果能够访问y虚拟地址的话，也能看到n