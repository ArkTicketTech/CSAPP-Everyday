进程
进程树：
● 所有的进程都是从父进程 fork 过来的，父进程就是系统启动的 init 进程1 号进程。
● 系统启动之后，init 进程会启动很多的 daemon 进程，为系统运行提供服务
线程：
● 通过调用 pthread_create 创建线程
线程数据：
● 线程栈上的本地数据，比如函数执行过程中的局部变量。函数的调用会使用栈模型，每个线程都有自己的栈空间
  ○ 栈大小可以通过命令 ulimit -a 查看，默认线程栈大小为 8192（8MB）。可以使用命令 ulimit -s 修改
● 整个进程里共享的全局数据。例如全局变量，在一个进程中是共享的。
● 线程私有数据