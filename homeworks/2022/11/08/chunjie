6.4.5 有关写的问题
  写回有两种方式，一种是在数据更改时直接更新缓存，简单但会引起总线流量增加；一种是要被写入内存的时候更改，但会增加复杂度。
  
6.4.6 一个真实的高速缓存层次结构的解剖
  实际上高速缓存也会存指令，根据高速缓存缓存的内容可以分为 i-cache, d-cache, 统一的高速缓存
  
6.4.7 高速缓存参数的性能影响
  有很多指标来衡量高速缓存的性能，例如不命中率，命中率，命中时间，不命中处罚等。
  高速缓存大小，块大小，相联度和写策略都会显著影响高速缓存的性能。
