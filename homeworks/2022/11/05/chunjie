6.4.3 组相连高速缓存
  直接映射高速缓存冲突不命中源于每组只有一行。
  一个 1 < E < C/B 的组相连高速缓存被称为 E 路组相连高速缓存。
  行匹配和字选择会复杂一些，行相同时需要对比字的 key。
  不命中时行替换算法很难被程序员在代码中使用，通常也是 LRU LFU 等策略。
  
6.4.4 全相联高速缓存
  全相联高速缓存中只有一个组，因此没有组索引位，行匹配和字选择和组相连高速缓存模式相同。
  因为并行搜索所有行和字匹配很昂贵，全相联高速缓存只适合做小的缓存。
