5.5 减少过程调用
这里的例子很有意思，combine3比起combine2性能并没有提升。
通过看汇编代码可以发现，这里的读取和写入操作太多了。
通过引入局部变量acc，就可以去掉这种不必要的读写。
事实上，combine4比起combine3的提升理论上可以通过编译器完成，但编译器并没有这样做。
但因为做这样的优化可能会引起不同的行为（具体可以看355页的对比）
所以编译器并没有这样做
然而，使用O3等级的优化，其效果也可以接近。其通过直接读取来减少存指令的耗时。

