5.2 表示程序性能
以前缀和的计算为例。
p346这里列出了两种计算前缀和的方式。
第一种十分熟悉，每个刷过算法题的同学应该都写过这个入门的基础题。
第二种就稍显不一样了，而且，根据复杂度的思想，这种计算方式似乎并没有使计算变得更简单。
但事实上，根据图5-2来看，第二种被称为“循环展开”的方式显然更好。
对于这两个函数的运行时间，是一个常数加上一个系数乘以元素个数。
其中这个系数就被称为CPE。
5.3 程序示例
话说有了ChatGPT后看代码真的好方便……直接复制就行了。
这里讲解了一下向量结构体以及使用的示例，略。
