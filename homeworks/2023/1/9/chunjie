7.8 可执行目标文件
  可执行目标文件格式类似于可重定位目标文件，包含一个描述文件结构的 ELF 头，第一条指令的地址，已经被重定位到最终运行内存的数据，还有一个初始化的函数。
  ELF 很容易映射到内存，并有内存对齐的要求。
  
7.9 加载可执行文件
  shell 会调用成为加载器的操作系统代码来运行可执行文件，加载器将代码和数据复制到内存中，然后跳到第一条指令来运行程序。
  每个 linux 程序都有一个运行时内存映像，代码段总是从 0x400000 开始，后面是数据段和运行时堆，用户栈则从最大合法地址 2^48-1 向较小内存增长。2^48 开始是内内存。
