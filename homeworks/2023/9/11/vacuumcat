继续来看虚拟内存。
9.7.2 Linux虚拟内存系统
这一节内容已经看过好几次了（
依然是之前的内容，那么本节就当是复习吧。
首先是地址，虚拟内存地址的内核虚拟内存在高地址。
进程虚拟内存则在低地址。
从高到低依次是：
与进程相关数据结构（页表、ta
  sk、mm、内核栈等）
物理内存
内核代码和数据
用户栈（进程虚拟地址从这里开始）
共享库的内存映射区域
运行时堆
未初始化的数据
已初始化的数据
代码

其中，task_struct为任务结构
存放着如pgd、mm等字段
pgd指向一级页表，而mm存放一些虚拟内存片段信息
