8.4.3 回收子进程
  进程被终止的时候会等待父进程来回收，这之前称为僵尸进程。
  如果父进程也终止了，会有 init 进程来进行回收，init 进程是系统初始化的第一个进程。
  进程通过 waitpid 这个函数来等待子进程结束，waitpid 可以等待某一个进程，也可以等待所有进程的合集。
  waitpid 有各种参数来控制父进程的状态，等待的行为和错误时的行为。
  
8.4.4 让进程休眠
  sleep 会将进程挂起一段时间，sleep 会返回 0 或者剩余的时间，后者当 sleep 被中断时发生。
  pause 可以将进程挂起直到被唤醒。
