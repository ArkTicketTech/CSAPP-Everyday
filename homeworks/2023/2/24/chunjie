8.5.3 接收信号
  进程切换到用户模式时会检查未被阻塞的待处理信号的集合，非空情况下内核会选择最小的信号来接收。
  信号有一些默认行为，进程终止，进程终止并转储内存，进程挂起，忽略信号。
  信号处理程序会被其他信号处理程序中断。
  
8.5.4 阻塞和接触阻塞信号
  linux 提供阻塞信号的隐式和显式的机制。
  隐式阻塞机制是默认阻塞任何当前处理程序正在处理信号类型的待处理的信号。
  显式阻塞机制是调用 sigprocmask 函数和相应辅助函数明确的阻塞和接触阻塞选定的信号。
  
