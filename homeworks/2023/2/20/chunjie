8.4.5 加载并运行程序
  execve 函数在当前进程的上下文加载并运行一个新程序，只有在报错时才返回到调用程序。
  执行时用户栈从高到低是参数和环境字符串，以 null 结为的指针数组，每个指针指向一个环境变量字符串，以 null 结尾的参数数组，每个元素指向一个参数字符串。
  
8.4.6 利用 fork 和 execve 运行程序
  shell 就是一个不断获取用户输入的命令，然后调用子进程来做相应处理的程序。
