进程回收顺序是一种 非确定性行为，不能够假设进程的回收顺序以实现预期的目的。
当然，可以通过存储并指定pid的方法来消除这种不确定性。
8.4.4 让进程休眠
sleep可以实现进程休眠。
pause也是类似的，但它不是等待一个固定时间，而是等待一个信号。
8.4.5 加载并运行程序
这一节说明了为什么我们见到的c++代码经常会有形如**argv这样的参数。
很显然，内核的程序运行方式比应用层的运行要更为复杂与精细。
