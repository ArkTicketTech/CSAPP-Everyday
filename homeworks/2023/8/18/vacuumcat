8.16 非本地跳转
非本地跳转的概念并不难理解，有点类似于汇编中jmp的概念。
只不过这里需要使用env来存储当前的调用环境。
其中，setjmp会调用一次但返回多次。
这可能不太容易理解。
看图8-43就可以明白了。
第一次调用setjmp时，程序会正常向下走，而这时返回值是0，走case0。
然后正常走到bar中，由于error2是1，这时会直接返回到最近保存的setjmp处。
这时候返回值就是2了。
图8-44显示了另一种应用：使信号处理程序到达一个特殊的代码位置。
其逻辑是类似的，不再赘述。
