7.4 可重定位目标文件
上次看到了这里，但没有对各个节进行深入的探索。
ELF头中包含了生成该文件的系统的字的大小和字节顺序。
而ELF头中剩下的部分包含了ELF头的大小、目标文件的类型、机器类型
节头部表的文件偏移，以及节头部表中条目的大小和数量。
而夹在ELF头和节头部表中间的都是节。
.text为已编译程序的机器代码
.rodata为只读数据
.data为已初始化的全局和静态C变量
.bss为未初始化的全局和静态C变量，以及所有被初始化为0的全局或静态变量
.symtab是一个符号表，存放在程序中定义和引用的函数和全局变量的信息
有点太多了。。。略。后面会细讲。

7.5 符号和符号表
符号有三种：
由模块定义并能被其它模块引用的全局符号。
由其他模块定义并被模块m引用的全局符号。
只被m引用和定义的局部符号。
看这里这段程序，是个很好的例子。
P470
