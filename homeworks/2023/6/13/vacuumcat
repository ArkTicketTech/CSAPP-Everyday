6.直接映射高速缓存中的冲突不命中
本节主要讲解了一个看起来似乎不会出现性能问题的程序由于出现内存抖动导致性能下降的问题。
解决方式也很简单，加一个填充即可。
6.4.3 组相联高速缓存
这个的设计思想也很简单，就是把每组的数量扩展一下。
通过遍历和标记的比对确认是否有缓存。
没有的话，使用替换策略来进行内存的替换。
6.4.4 全相联高速缓存
这。。。感觉这个策略好奢侈。
本质上来讲，和其他策略并无不同。
