8.5.6 同步流以避免讨厌的并发错误
  流可能交错的数量和指令数量呈指数关系，一个很难的问题在于同步并发流，在正确的前提下得到最大的并发。
  一些信号可能在父进程和子进程之间有并发，如果没有正确的得到处理会出现子进程不正确的被处理。
  
8.5.7 显式的等待信号
  有时候主程序需要显式等待某个信号处理程序完成，一个方式是通过自旋等待的方式，但这样代码被正确执行时效率不高，合适的方式是用 sigsuspend 函数。
  sigsuspend 函数用 mask 替换当前阻塞集合，然后挂起进程，直到终止或者恢复原有的阻塞集合。
  
