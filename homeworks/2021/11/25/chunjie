chap 3.6.2 访问条件码
  条件码通常不会直接读取，常见用法有三种：
  1. 根据条件码的某种组合将一个字节设置为 0 或 1。
  2. 跳转到程序的某一个位置。
  3. 有条件的传输数据。
  某些机器指令可能有多个名字。
  
chap 3.6.3 跳转指令
  正常情况下指令会按顺序执行，jump 指令可以将程序跳转到一个全新的执行位置。
  跳转目的地通常用一个 label 指明。
  jmp 指令有很多条件变种。
  
