chap 3.4.4 压入和弹出栈操作
  push 和 pop 操作可以将数据压入/弹出程序栈。
  程序栈是内存中向下增长的"数组"，栈顶元素的地址是最低的，%rsp 寄存器中存放栈顶地址。
  push 操作等价于 sub $8, %rsp + mov %rbp, (%rsp)，pop 反之，区别就是机器代码中是一条指令。
  实际上栈内任意位置可以用标准内存寻址的方法来找到，因为栈和程序代码和程序数据都是在同一内存中的，并且栈是连续的。
  
chap 3.5 算数和逻辑操作
  只有 leaq 没有指示大小的变种，其余的算数操作都有大小的变种。
  
chap 3.5.1 加载有效地址
  leaq 其实是 movq 的变形，区别是 leaq 其实不是从内存中读取数据加载到寄存器里，而是将数据的内存地址加载到寄存器里，相当于 c 的 & 操作。
  leaq 指令可以用来描述一些普通的算数操作，会被编译器用来编译加法和有限形式的乘法。
  
