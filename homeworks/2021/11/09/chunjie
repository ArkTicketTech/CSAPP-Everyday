chap 3.4.2 数据传输指令
  mov 指令可以跟各种后缀作为指示，比如 b/w/q 指示数据大小，s/z 指示如何填充目的中剩余的字节。
  cltq 指令总是以 %eax 作为源，%rax作为目的。编码更为紧凑。

chap 3.4.3
  c 语言中间接引用地址就是将指针放在寄存器里，然后在内存引用中使用这个寄存器。局部变量一般都是保存在寄存器中。
  * 执行间接引用，& 执行取址。
  
