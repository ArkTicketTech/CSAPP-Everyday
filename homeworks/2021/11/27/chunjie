 chap 3.6.4 跳转指令的编码
  汇编代码中跳转目标用符号书写，汇编器和链接器会产生跳转目标的适当编码。
  1. pc-relative，将目标代码和紧跟的下一条指令的差作为编码。
  2. 给出跳转代码的绝对地址。
  pc 中按惯例是当前指令下一条指令的地址，早起实现是会把更新 pc 作为执行指令的第一步。

chap 3.6.5 用条件控制来实现条件分支
  将条件表达式和语句翻译到机器代码，最常用的方式是结合有条件和无条件跳转。
