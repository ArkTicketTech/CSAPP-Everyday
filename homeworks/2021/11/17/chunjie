chap 3.6 控制
  机器代码提供两种低级机制来实现条件的行为，测试数据，然后根据结果改变控制流。例如 jump 指令可以改变一组代码执行的顺序。
  
chap 3.6.1 条件码
  cpu 有一组单个位的条件码寄存器，描述了最近的算数或逻辑操作的属性。
  CF: 进位。表示最高位的进位。
  ZF：零标志。
  SF：符号标志。表示负数。
  OF：溢出标志。
  除了 leaq 指令之外的算数运算指令都会更新设置条件码。
  cmp 和 sub 指令的差别在于，cmp 只更新条件码寄存器，没有实际的目的寄存器。test 和 add 也是如此。
