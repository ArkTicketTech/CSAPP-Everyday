chap 3.6.7 循环
  编译器主要用条件测试和跳转来实现循环。
  do-while 循环模式可以理解为在循环最后加了满足条件 goto 循环第一句的跳转。
  while 循环可以理解为先进行一次判断，如果满足了条件 goto 循环第一句的跳转。
  while 循环可以翻译为先 goto test，然后 test 返回循环，或者 !test 跳转到循环外。
  在较高编译模式下编译器会采用后一种，来优化初始的测试。
  
